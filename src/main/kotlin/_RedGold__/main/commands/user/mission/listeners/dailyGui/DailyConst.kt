package _RedGold__.main.commands.user.mission.listeners.dailyGui

import _RedGold__.main.managers.playerData.variableManager.MissionInfo

object DailyConst {
    fun setMission(info: MissionInfo, ) {

    }
}