# 규칙

## 공통 규칙:
- 모든 클래스는 object 클래스에서 가져오는 변수나 함수를 사용할 때 `(object클래스 명).(함수/변수 명)` 으로 가져와야 한다.
- (main.functions은 제외)
- UUID.fromString() 대신 .toUUID() 를 사용한다.
- 직접 개발한 함수/변수에서 throw를 사용할 때 ```PluginException("오류 메시지", 오류 심각성 Enum)``` 클래스 사용한다.
- Enum을 불러올 때 `(Enum 클래스 명).Enum 데이터 그거` 이런식으로 불러와야 한다.
- 가능하면 스케줄에서 Later 대신 plugin.launch 안에 delay를 사용 

## 매니저 규칙:
- PlayerManager: 플레이어의 PlayerData 캐시 관리 당담
- DatabaseManager: SQL 쿼리 당담
- DataManager: PlayerData 클래스 당담
- **데이터를 불러올 때 PlayerManager의 player.data를 사용**

## DB/메모리 규칙:
- DB 관련 작업은 무조건 **비동기**에서 진행한다.
- DB 저장은 `서버 종료`/`유저 퇴장`/`10분 주기`로 데이터를 저장한다.
  - `서버 종료`/`유저 퇴장`/`10분 주기`에 데이터를 저장하는 코드는 main.managers 폴더 내에서 진행한다.
- 한 번 불러온 데이터는 메모리에서 삭제를 안한다.
- `loadXXX()`(DB -> RAM), `saveXXX()`(RAM -> DB), `fetchXXX`(캐싱되어 있으면 캐싱 데이터 수정, 없을 시 DB 데이터를 수정)
- main.managers에 있는 모든 Listener은 중요도가 EventPriority.LOWEST 여야 한다.
- 만일 다른 테이블에서 사용하는 값을 넣는 컬럼이 있다면 `(그 테이블 명, 복수형 생략 가능)_(가져온 컬럼 이름)` 이런 식으로 작성한다.

## 네이밍 규칙:
- 클래스 명은 PascalCase 표기 법을 사용한다.
  - Data클래스는 끝에 Data를, Object클래스는 끝에 Object를 붙인다.
  - Enum클래스는 끝에 Enum을 붙인다.
- 함수/변수 명은 camelCase 표기 법을 사용한다.
- DB 컬럼 이름은 snake_case 표기 법을 사용한다.
- 상수는 대문자 SNAKE_CASE 표기 법을 사용한다.

- Boolean 타입은 변수명 시작에 `is`/`has` 를 붙인다.
- UUID 타입은 변수명 끝에 `UUID`를 붙인다.
- List/Map 타입은 명확한 타입이 필요 할 때만 변수명 끝에 `List`/`Map` 을 붙인다.
  - 그 외에는 복수형으로 쓴다.
- 비동기 함수는 함수명 끝에 `Async`를 붙인다.
- 타입을 `A`에서 `B`로 바꿀 때 가능하면 `toB()` 또는 `asB()` 를 사용한다.

## ROOT 폴더 규칙
- *.main: 최상위 폴더이며 오직 Main 클래스와 상위 폴더만 존재
- main.commands: 폴더 안에는 `(펄미션)/(명령어)/` 형식 안에서 개발
- main.events: 폴더 안에는 `(이벤트 명)/` 형식 안에서 개발
- main.functions: 폴더 안에는 유용하고 잡다한 기능들이 들어있음
- main.loads: 리플렉션이나 어노테이션을 이용한 **자동 로딩 시스템 전용 공간**. 개발자가 직접 비즈니스 로직을 작성하지 않고, 프레임워크 수준의 코드만 보관
- main.managers: DB/메모리 관리 함수나 상수, 변수들이 들어있는 폴더
- main.listeners: 폴더 안에서 Listener은 `(클래스 명 폴더)/(클래스 명)Listener.kt`, 상수는 `(클래스 명 폴더)/(클래스 명)Const.kt`에 넣어야 한다.
  - 여러 폴더에서 사용되는 상수가 있을 시 `listeners/GlobalConst.kt`에 넣어야 한다.
  - 만일 다른 클래스에서 바뀌는 데이터 그런 거는 `(클래스 명)Value.kt`에 object로 넣는다


가능하면 plugin.task task@{} 이런식으로 사용좀